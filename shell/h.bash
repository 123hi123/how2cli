# how2cli shell integration for bash
# Add to ~/.bashrc: source /path/to/how2cli/shell/h.bash

# Bash doesn't have noglob, but ? in arguments is less problematic
# This alias is optional, for consistency with zsh wrapper
alias h='command h'
alias ht='command ht'
